@startuml
skinparam dpi 300
start
:Вычислить хеш ключа;
:Найти индекс (с учётом стратегии);
if (Ключ уже существует?) then (да)
  :Обновить значение;
  stop
else (нет)
  if (Требуется перехеширование?) then (да)
    :Перехешировать таблицу;
  endif
  if (Политика == Chaining) then (да)
    :Добавить в начало списка;
  else (Открытая адресация)
    :Пробирование (линейное/квадратичное);
    :Найти свободный слот;
    :Вставить элемент;
  endif
  :Увеличить счётчик активных;
  stop
endif
@enduml
